import { Provider } from '@angular/core';

/**
 * Shared providers - reusable UI components, directives, pipes
 * This module can be imported in feature modules that need shared functionality
 */
export const SHARED_PROVIDERS: Provider[] = [
  // Add shared providers here
  // Example:
  // provideCurrencyPipe(),
];
