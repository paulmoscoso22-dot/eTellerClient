import { Provider } from '@angular/core';

/**
 * Core providers - singleton services, guards, and interceptors
 * This module should be imported only once in the root application
 */
export const CORE_PROVIDERS: Provider[] = [
  // Add core services here
  // Example:
  // provideCacheService(),
  // provideDynamicTitleService(),
];
